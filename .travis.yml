dist: trusty
language: python
stages:
  - compile
  - test
  - name: deploy
    if: branch = master
jobs:
  include:
    - stage: compile
      env:
        - distro=alpine
        - distro=ubuntu
      script:
        - echo 'warming up'
        - echo "compileing?"
    #- script: echo "Ordered dict???"
    - stage: test
      script:
      - echo "Testing"
