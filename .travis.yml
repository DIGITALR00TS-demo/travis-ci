dist: trusty
language: python
stages:
  - compile
  - test
  - name: deploy
    if: branch = master
jobs:
  include:
    - stage: compile
      script:
      - echo "compileing?"
    - script: echo "Ordered dict???"
    - stage: test
      script:
      - echo "Testing"
